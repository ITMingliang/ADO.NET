1.相同  执行查询，将数据读取出来，一个或多个


SqlDataReader  提供一种从数据库中读取数据的流的方式

SqlDataAdapter DataSet与数据库之间的一个桥接器.  Fill Update

2、性能对比：速度  占用内存   连接状态  适用数据量  读取方式   是否可读

 速度:        快             慢  （数据量小的情况，速度快慢不明显）
 
 数据量      小             大

 内存        小             大

 连接     一直占用           断开与连接

 读取方式  从头读到尾        一次性加载到内存中（一个或多个）   
           一条一条读         任意读取，
          读一条丢一条
          只向前不能后退
           （即时存储）
            不灵活           灵活
          只读，不能修改     可读可写


不要求随意读取，不修改，数据量小的情况下，SqlDataReader  ---速度快，占用内存小
可以随意读取，可以修改，数据量大的情况---SqlDataAdapter ---灵活性大，占用内存大，不一直占用连接

Leah QQ 2159674643

下节课：Ado.Net调用数据库事务